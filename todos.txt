Фичи:
	
	[+] Добавить детальное описание ошибок в файлах логов.
	
	[ ] Добавить автоматическую подстановку константных значений из файла
	таблицы (static.xls).

Регулярные ошибки:
	
	[+] Не отлавливается ситуация с передачей пустой строки в качестве пароля
	при аутентификации пользователя. Программа завершается с ошибкой.

	[+] При вызове программы через TestStand не возможно сохранить
	последовательность тестов. Ошибка заключается в том, что приложение не
	может вызвать sqlite3 для инициализации бд? Проинициализированная бд не
	устраняет ошибку.
	На самом деле ошибка в переполнении

		В планах переделать команду test так, чтобы она работала так,
		чтобы стало возможным постепенное заполнение данных

		+ Создание теста "по-умолчанию"

	[+] При первом запуске бд не инициализируется.
		
		Проблемы были в файле model.sql. Решено.

	[+] Не обновляется id последовательности тестов.

	[ ] Программа не отдаёт файл сессии обратно. Наблюдается после создания
	новой тестовой последовательности. + Проявляется после попытки входа.

		После 19.03.2020 ошибка не наблюдалась.
	
	[ ] При сохранении тестов сохраняется только один из всех. При этом
	дата начала теста не устанавливается (1.01.0001 0:00:00).

	[ ] При повторном запуске программы возникает ошибка в результате которой
	управление блокируется, в консоль ничего не выводится, и ввести что-либо
	тоже не получается.
	
	[ ] Ошибки с первой попыткой авторизации в программе через консоль.
	
	[ ] Ошибки связанные с кодировкой. В графическом приложении необходимо
	настроить вывод сообщений при выполнениии sql запросов.

Невоспроизводимые ошибки:
	
	Иногда при создании файла дампа коммандой dump-use Метод Session.Close
	может выбрасывать исключение о невозможности доступа к файлу .session
	потому что он занят другим процессом.